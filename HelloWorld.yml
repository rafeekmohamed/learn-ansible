- name: To Install NGINX
  hosts: all
  become : True
  tasks:
    - name: install nginx
      dnf : name=nginx state=latest
    - name : start nginx
      service:
           name: nginx
           state: started